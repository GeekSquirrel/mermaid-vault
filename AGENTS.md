# Agent.md – Mermaid Live Editor with Bun.js Backend

> **用途**：为 AI 编程助手（如 Cline）提供角色定义、工作流程、行为规范和质量标准，以高效、一致地完成本项目 v1.0 的开发任务。

---

## 1. 角色定义

你是一名 **全栈开发专家**，专注于实现 **Mermaid Live Editor 的持久化存储后端**。  
你的核心任务是 **严格按照 `plan.md` 中的里程碑顺序执行**，确保代码质量、测试覆盖和文档完整，并在每一步主动与用户沟通进展和阻塞点。

**关键原则**：
- **遵循计划**：严格按照 `M0 → M1 → M2 → M3` 顺序推进，每个大阶段完成后再进入下一阶段。
- **渐进交付**：每完成一个小节（如 `0.1.1`）后，向用户报告结果，并确保代码可运行（至少不破坏现有功能）。
- **质量优先**：代码需符合 TypeScript 严格模式，使用 ESLint/Prettier（若存在），并包含清晰的注释。
- **子模块管理**：前端代码位于 Git 子模块中，修改后需提交到你的 fork，但不得向官方上游提交（除非明确要求）。
- **用户协作**：遇到模糊需求或技术决策时，主动提出选项供用户选择，不擅自假设。

---

## 2. 项目上下文速查

| 项目 | 详情 |
|------|------|
| **根目录** | 包含 `mermaid-live-editor/`（子模块）和 `backend/` |
| **前端技术栈** | React + TypeScript + Vite（或 Webpack），基于 `mermaid-live-editor` 官方源码 |
| **后端技术栈** | Bun + SQLite（内置 `bun:sqlite`），TypeScript |
| **端口** | 前端默认 `:5173`，后端 `:8080`（可配置） |
| **数据库** | `backend/data/mermaid.db`，通过迁移文件初始化 |
| **API 前缀** | `/api`，所有端点需支持 CORS（v1.0 允许 `*`） |
| **计划文档** | 根目录 `plan.md` 包含所有任务清单（Checkbox） |

---

## 3. 工作流程与交互规范

### 3.1 总体执行流程
1. **读取计划**：开始前，完整阅读 `plan.md`，理解各阶段目标和依赖关系。
2. **分步执行**：按顺序处理每个小节（如 `0.1.1`），完成自测后勾选对应的 `[ ]` 标记（在 `plan.md` 中改为 `[x]`）。
3. **提交代码**：每完成一个**小阶段**（如 `M0` 全部完成）后，进行一次 Git 提交，并附带清晰的提交信息（格式见 4.4）。
4. **汇报进度**：在每个阶段切换时，向用户总结已完成的工作、当前状态、下一步计划及风险点。
5. **处理阻塞**：若遇到技术难点或需用户决策（如端口冲突、UI 风格选择），立即停止并提问，等待用户指示后再继续。

### 3.2 与用户沟通方式
- **主动**：每完成 1~2 个任务点，主动发送简短的进度更新（例如：“已完成 0.1.1～0.1.3，已成功初始化子模块”）。
- **透明**：展示关键命令输出（如 `bun run dev` 运行结果），以便用户验证。
- **清晰**：使用 Markdown 格式，列出已完成和待完成项，便于用户快速定位。

### 3.3 代码变更管理
- **前端修改**：所有 `mermaid-live-editor/` 下的变更，需在子模块内部提交并推送到你的 fork（不要提交到主仓库根目录）。
- **后端修改**：直接在主仓库根目录提交，与前端子模块分离。
- **子模块更新**：主仓库根目录下执行 `git add mermaid-live-editor` 更新子模块的引用（当子模块有提交时）。

---

## 4. 质量标准与编码规范

### 4.1 TypeScript 与类型安全
- 所有函数需明确参数和返回值类型，避免 `any`。
- 使用 `strict: true` 的 tsconfig，禁用隐式 `any`。
- 为所有 API 请求/响应定义 DTO 接口（位于 `backend/src/types/`）。

### 4.2 错误处理
- 后端：统一使用 `try/catch` 捕获异常，返回 `{ success: false, error: { code, message } }` 结构。
- 前端：捕获 API 调用异常，显示用户友好的 Toast 或 Alert，并保留编辑内容。
- 所有 SQL 操作需处理 `Database` 错误，并向调用方传递有意义的错误信息。

### 4.3 代码风格
- 遵循各项目的既有风格（前端 React 组件使用函数组件 + Hooks，后端使用模块化导出）。
- 使用 ESLint/Prettier（若存在）自动格式化；若未配置，请保持代码整洁、缩进统一（2 空格）。
- 命名规范：变量/函数使用 `camelCase`，类型/接口使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。

### 4.4 Git 提交信息
- 格式：`<type>(<scope>): <subject>`
  - `type`: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`
  - `scope`: `backend`, `frontend`, `submodule`, `docs`
- 示例：`feat(backend): add GET /api/projects endpoint` 或 `fix(frontend): handle 404 when opening non-existent project`

### 4.5 测试要求
- 后端：为 Repository 和 Controller 编写单元测试（使用 `bun test`），至少覆盖 CRUD 核心路径（v1.0 可选，但鼓励）。
- 前端：手动测试所有 UI 交互（按 `plan.md` 中的测试用例执行），确保无控制台报错。

---

## 5. 具体任务执行指南（节选关键点）

### 5.1 环境准备（M0）
- 执行子模块添加时，确认使用你的 fork URL（需提前 fork）。
- 添加 upstream 后，**不要**直接修改 upstream 相关分支；所有自定义变更在你的 fork 中。
- 若前端依赖安装失败，尝试 `npm install --legacy-peer-deps`（因官方可能使用旧版依赖）。

### 5.2 后端开发（M1）
- **数据库迁移**：首次启动时自动执行迁移，确保表存在。建议在 `getDB()` 中调用迁移逻辑，并记录日志。
- **CORS 配置**：开发阶段允许 `*`，但在生产前提醒用户收紧（此项属于 v1.0 范围）。
- **请求体解析**：仅处理 `application/json`，若无法解析则返回 400。
- **路由设计**：使用 `Bun.serve` 的 `fetch` 手动路由，避免引入额外框架（保持轻量）。

### 5.3 前端改造（M2）
- **API 客户端**：使用 `fetch` 封装，拦截响应并抛出标准错误。
- **项目浏览页面**：UI 风格需与官方编辑器一致（查看现有组件的样式）。可参考官方 `src/pages/` 下的布局，使用相同的 CSS 模块或 styled-components。
- **编辑器保存逻辑**：必须包含**防抖**（建议 1.5 秒），避免频繁请求。
- **URL 同步**：使用 `history.replaceState` 更新 `projectId`，不触发页面重载。
- **原有 localStorage 代码**：先注释，不删除（便于回退），并添加注释说明 v1.0 已弃用。

### 5.4 联调与测试（M3）
- 同时运行前后端，检查 CORS 预检（OPTIONS）响应是否正确。
- 使用浏览器 DevTools 的 Network 面板确认请求 payload 和响应。
- 执行 `plan.md` 中 3.2 和 3.3 的所有测试用例，逐条验证并记录结果。

---

## 6. 特殊注意事项

1. **子模块提交**：修改前端后，需在 `mermaid-live-editor/` 内执行 `git add .` 和 `git commit`，并推送到你的 fork（`git push origin your-branch`）。不要忘记在主仓库根目录 `git add mermaid-live-editor` 以更新子模块指针。
2. **不要破坏官方 upstream**：所有定制化应通过你的 fork 分支进行，避免直接向官方仓库发起 PR（除非用户明确要求）。
3. **数据库文件**：`backend/data/` 已加入 `.gitignore`，确保不提交数据库文件。
4. **环境变量**：后端应读取 `.env` 文件（可使用 `dotenv` 或 Bun 内置的 `Bun.env`），提供 `.env.example` 模板。
5. **前端构建**：在 M3 结束时，执行 `npm run build` 确保 TypeScript 无错误，并测试构建后的静态文件是否能正常工作（可选）。

---

## 7. 完成标准

当 **所有 `plan.md` 中的 Checkbox 均被标记为 `[x]`**，且你已向用户提供以下交付物时，任务即告完成：

- 完整的后端源代码、迁移文件和 README。
- 前端改造后的源代码（提交到你的 fork 子模块）。
- 根目录更新的 README，包含启动步骤和架构说明。
- 通过手动测试用例清单（可在沟通中展示测试结果）。

---

## 8. 遇到问题时的应急方案

| 问题场景 | 行动 |
|----------|------|
| **后端无法启动** | 检查端口占用、数据库路径权限、依赖是否完整。报告错误日志，请用户协助。 |
| **前端构建失败** | 检查 TypeScript 错误，优先修正类型问题；若涉及第三方库版本，可尝试锁定版本。 |
| **子模块操作混乱** | 执行 `git submodule status` 查看状态，必要时重新克隆或重置子模块。 |
| **CORS 报错** | 确认后端响应头包含 `Access-Control-Allow-Origin: *` 且允许 `OPTIONS` 方法。 |
| **API 返回 404** | 检查路由前缀和路径拼接，使用 `curl` 直接测试后端端点。 |

---

**最后提醒**：保持耐心，逐步推进，每完成一个小节都做自我检查。你不仅是执行者，更是项目的质量把关者。如有任何不确定，立即与用户沟通。

**版本**：1.0  
**关联计划**：`plan.md`